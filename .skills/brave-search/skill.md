Search the web via the Brave Search API and return structured results.

Use this skill when an agent needs to:
- Research a topic and pull current information into a wiki page
- Find documentation, release notes, or changelogs for a dependency
- Answer questions that require up-to-date information beyond training data

Requires a `BRAVE_API_KEY` secret (free tier available at brave.com/search/api).
