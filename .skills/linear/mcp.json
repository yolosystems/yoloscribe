{
  "mcpServers": {
    "linear": {
      "command": "node",
      "args": ["${AGENT_RUNNER_HOME}/linear_mcp_wrapper.js"],
      "env": {
        "LINEAR_API_TOKEN": "${LINEAR_API_KEY}"
      }
    }
  }
}
