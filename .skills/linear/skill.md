Query and create Linear issues, cycles, projects, and team updates.

Use this skill when an agent needs to:
- Look up open issues or a team's current cycle
- Create or update issues from wiki content
- Summarise a sprint or project status into a wiki page
- Cross-reference wiki runbooks with tracked bugs

Requires a `LINEAR_API_KEY` secret (created in Linear → Settings → API).
