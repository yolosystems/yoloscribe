apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentscribe-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentscribe-backend.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "agentscribe-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agentscribe-backend.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "agentscribe-backend.serviceAccountName" . }}
      {{- if .Values.ghcr.pat }}
      imagePullSecrets:
        - name: {{ include "agentscribe-backend.fullname" . }}-ghcr
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentscribe-backend.secretName" . }}
                  key: {{ .Values.existingSecretKey }}
            - name: S3_BUCKET
              value: {{ .Values.config.s3Bucket | quote }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.config.allowedOrigins | quote }}
            - name: SQS_QUEUE_URL
              value: {{ .Values.config.sqsQueueUrl | quote }}
            - name: SQS_INDEXING_QUEUE_URL
              value: {{ .Values.config.sqsIndexingQueueUrl | quote }}
            - name: S3_VECTORS_BUCKET
              value: {{ .Values.config.s3VectorsBucket | quote }}
            - name: S3_VECTORS_INDEX_NAME
              value: {{ .Values.config.s3VectorsIndexName | quote }}
            - name: BEDROCK_EMBEDDING_MODEL
              value: {{ .Values.config.bedrockEmbeddingModel | quote }}
            - name: EKS_OIDC_PROVIDER
              value: {{ .Values.config.eksOidcProvider | quote }}
            - name: AWS_ACCOUNT_ID
              value: {{ .Values.config.awsAccountId | quote }}
            - name: AWS_REGION
              value: {{ .Values.config.awsRegion | quote }}
            - name: K8S_NAMESPACE
              value: {{ .Values.config.k8sNamespace | quote }}
            {{- if .Values.webhookSecret }}
            - name: WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentscribe-backend.fullname" . }}-webhook
                  key: webhook-secret
                  optional: true
            {{- end }}
            - name: SUPABASE_URL
              value: {{ .Values.config.supabaseUrl | quote }}
            {{- if .Values.supabaseServiceRoleKey }}
            - name: SUPABASE_SERVICE_ROLE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentscribe-backend.secretName" . }}
                  key: supabase-service-role-key
            {{- end }}
            {{- if .Values.config.cloudfrontDomain }}
            - name: CLOUDFRONT_DOMAIN
              value: {{ .Values.config.cloudfrontDomain | quote }}
            {{- end }}
            {{- if .Values.ingress.host }}
            - name: OAUTH_REDIRECT_URI
              value: {{ printf "https://%s/oauth/callback" .Values.ingress.host | quote }}
            {{- end }}
            {{- if .Values.config.mcpBaseUrl }}
            - name: MCP_BASE_URL
              value: {{ .Values.config.mcpBaseUrl | quote }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
