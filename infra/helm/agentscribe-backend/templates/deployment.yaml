apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentscribe-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentscribe-backend.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "agentscribe-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agentscribe-backend.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "agentscribe-backend.serviceAccountName" . }}
      {{- if .Values.ghcr.pat }}
      imagePullSecrets:
        - name: {{ include "agentscribe-backend.fullname" . }}-ghcr
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentscribe-backend.secretName" . }}
                  key: {{ .Values.existingSecretKey }}
            - name: S3_BUCKET
              value: {{ .Values.config.s3Bucket | quote }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.config.allowedOrigins | quote }}
            - name: SQS_QUEUE_URL
              value: {{ .Values.config.sqsQueueUrl | quote }}
            - name: EKS_OIDC_PROVIDER
              value: {{ .Values.config.eksOidcProvider | quote }}
            - name: AWS_ACCOUNT_ID
              value: {{ .Values.config.awsAccountId | quote }}
            - name: AWS_REGION
              value: {{ .Values.config.awsRegion | quote }}
            - name: K8S_NAMESPACE
              value: {{ .Values.config.k8sNamespace | quote }}
            {{- if .Values.webhookSecret }}
            - name: WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentscribe-backend.fullname" . }}-webhook
                  key: webhook-secret
                  optional: true
            {{- end }}
            - name: SUPABASE_URL
              value: {{ .Values.config.supabaseUrl | quote }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
