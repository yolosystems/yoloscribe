apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentscribe-discord-bot.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentscribe-discord-bot.labels" . | nindent 4 }}
type: Opaque
stringData:
  discord-bot-token: {{ .Values.discordBotToken | quote }}
  discord-aes-key: {{ .Values.discordAesKey | quote }}
